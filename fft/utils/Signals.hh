#ifndef _FFT_UTILS_SIGNALS_HH_
#define _FFT_UTILS_SIGNALS_HH_

namespace fft {
  namespace math {
    void signal(double& re, double& im, double frac);
  } // namespace math
} // namespace fft

#endif
